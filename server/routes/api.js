var express = require("express");
var router = express.Router();
var mongoose = require("mongoose");

// Models
var Usuario = require("../models/usuario");
// var Universidad = require("../models/universidad");
